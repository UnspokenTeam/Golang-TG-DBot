name: Deploy bot to VPS via GHCR

on:
  push:
    branches:
      - main
    paths-ignore:
      - .github
      - .devcontainer
      - .gitignore
      - .idea

permissions:
  contents: read
  packages: write
  actions: write

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.REGISTRY_URL }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ vars.REGISTRY_URL }}/${{ vars.REPO_URL }}/${{ vars.BOT_IMAGE_NAME }}:${{ github.sha }}
            ${{ vars.REGISTRY_URL }}/${{ vars.REPO_URL }}/${{ vars.BOT_IMAGE_NAME }}:prod
          cache-from: type=registry,ref=${{ vars.REGISTRY_URL }}/${{ vars.REPO_URL }}/${{ vars.BOT_IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ vars.REGISTRY_URL }}/${{ vars.REPO_URL }}/${{ vars.BOT_IMAGE_NAME }}:buildcache,mode=max

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          script: |
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            cd ${{ vars.WORK_DIR }}
            git pull origin main
            cd deployments
            docker compose -f docker-compose.prod.yml --env-file ./../.env --profile prod up -d
            docker image prune -f