name: Deploy bot to VPS via GHCR

on:
  push:
    branches:
      - main
    paths-ignore:
      - .github
      - .devcontainer
      - .gitignore
      - .idea

permissions:
  contents: read
  packages: write
  actions: write

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.REGISTRY_URL }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.REGISTRY_URL }}/${{ secrets.REPO_URL }}/${{ secrets.BOT_IMAGE_NAME }}:${{ github.sha }}
            ${{ secrets.REGISTRY_URL }}/${{ secrets.REPO_URL }}/${{ secrets.BOT_IMAGE_NAME }}:prod
          cache-from: type=registry,ref=${{ secrets.REGISTRY_URL }}/${{ secrets.REPO_URL }}/${{ secrets.BOT_IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ secrets.REGISTRY_URL }}/${{ secrets.REPO_URL }}/${{ secrets.BOT_IMAGE_NAME }}:buildcache,mode=max

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            cd ${{ secrets.WORK_DIR }}
            git pull origin main
            cd deployments
            docker compose -f docker-compose.prod.yml --env-file ./../.env --profile prod up -d
            docker image prune -f